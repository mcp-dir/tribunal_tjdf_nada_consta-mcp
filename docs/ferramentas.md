# Ferramentas

Tribunal TJDF: Nada Consta expõe 1 ferramenta (todas somente leitura).

### 1. `tribunal_tjdf_nada_consta_consultar`
**Input**: `cpf` (opcional), `cnpj` (opcional), `primeiro_nome` (opcional), `nome_mae` (opcional), `nome_pai` (opcional), `tipo_certidao`

Tribunal TJDF: Nada Consta, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Tribunal TJDF: Nada Consta
```
